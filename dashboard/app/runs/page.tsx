import { Shell } from "../components/Shell";
export default function Runs() { return <Shell><h1>Run history</h1><p className="muted">Imports, scrapes, optimization, publishing, and reconciliation runs.</p><div className="card empty">Run details are available from <code>/api/runs</code>.</div></Shell>; }

