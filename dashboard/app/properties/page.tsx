import { Shell } from "../components/Shell";
export default function Properties() { return <Shell><h1>Properties</h1><p className="muted">Manage bounds, seasonal and weekday factors, stay rules, discount references, and up to 30 competitor URLs through the properties API.</p><div className="card empty">Property editor is backed by <code>/api/properties</code>.</div></Shell>; }

