Small status pill — booking status, occupancy state, counts.

```jsx
<Badge tone="accent">Confirmed</Badge>
```

Tones: `neutral` (default), `success` (positive/active state), `danger` (needs attention), `accent` (brand emphasis, not a status), `inverse` (on dark surfaces).
