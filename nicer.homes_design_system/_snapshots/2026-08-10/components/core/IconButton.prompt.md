Circular icon-only button for toolbars and compact actions; always pass `label` for a11y.

```jsx
<IconButton label="Close" onClick={close} icon={<XGlyph/>} />
```

`active` shows a persistent sunken background (e.g. selected filter). Default size 36px.
