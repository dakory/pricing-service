Underline-style tab row for switching views within a screen (e.g. dashboard sections).

```jsx
<Tabs items={[{label:'Overview',value:'ov'},{label:'Calendar',value:'cal'}]} value={v} onChange={setV} />
```
